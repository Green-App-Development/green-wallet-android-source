
include ':app'
rootProject.name = "GreenApp"

setBinding(new Binding([gradle:this]))


evaluate(new File(
		settingsDir.parentFile,
		'wallet-sdk/.android/include_flutter.groovy'
))

include ':wallet-sdk'
project(':wallet-sdk').projectDir = new File('../wallet-sdk')
